{-|
Bool typeclass instances for equality, ordering, and arbitrary generation.

Provides basic instances for boolean values with standard semantics.
-}
{ equality = ./equality.dhall
, ordering = ./ordering.dhall
, arbitrary = ./arbitrary.dhall
}
